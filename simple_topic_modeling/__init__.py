"""Simple Topic Modeling: topic modelling that runs fully in the browser."""

from simple_topic_modeling.config import package_version

__version__ = package_version()
"""The installed version. A literal here goes stale, so the value comes from the metadata."""
